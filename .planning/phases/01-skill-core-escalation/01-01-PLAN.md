---
phase: 01-skill-core-escalation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/pbi/SKILL.md
  - .claude/skills/pbi/commands/deep.md
autonomous: true
requirements:
  - PROG-01
  - PROG-02
  - PROG-03
  - PROG-04
  - INTR-01
  - INTR-02
  - INTR-03

must_haves:
  truths:
    - "User submits free-text DAX request and skill attempts a solution immediately without asking questions"
    - "After user signals failure (negative/correction language), skill asks one targeted question about the blocking gap"
    - "Escalation question targets exactly the gap: business question, model state, or existing measures — not all three"
    - "After user answers the escalation question, skill retries automatically without prompting user to re-submit"
    - "User invokes /pbi deep and gets a full 3-question upfront intake"
    - "Existing subcommands (explain, format, new, audit, etc.) still work unchanged"
  artifacts:
    - path: ".claude/skills/pbi/SKILL.md"
      provides: "v4.0 router with catch-all handler, escalation behavior, deep routing"
      contains: "version: 4.0.0"
    - path: ".claude/skills/pbi/commands/deep.md"
      provides: "Deep mode command — full upfront intake stub"
      contains: "/pbi deep"
  key_links:
    - from: ".claude/skills/pbi/SKILL.md"
      to: ".claude/skills/pbi/commands/deep.md"
      via: "routing table deep keyword row"
      pattern: "deep.*commands/deep.md"
    - from: ".claude/skills/pbi/SKILL.md"
      to: ".pbi-context.md"
      via: "escalation state read/write"
      pattern: "Escalation State"
---

<objective>
Rewrite SKILL.md from v3.0 to v4.0 with progressive friction: solve-first catch-all default, targeted escalation on user failure signals, and `/pbi deep` entry point. Create the deep.md command file.

Purpose: This is the core behavioral change — the skill stops interrogating upfront and starts solving immediately. Escalation only fires when the user explicitly signals failure.
Output: Updated SKILL.md (v4.0), new commands/deep.md
</objective>

<execution_context>
@C:/Users/DeveshD/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/DeveshD/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-skill-core-escalation/01-CONTEXT.md

<interfaces>
<!-- Current SKILL.md structure the executor must preserve and extend -->

From .claude/skills/pbi/SKILL.md (v3.0):
- Frontmatter: name, description, version, disable-model-invocation, model, allowed-tools
- Detection Blocks: PBIP Detection, File Index, PBIR Detection, Git State, Session Context
- Routing table: keyword-based dispatch to commands/*.md files
- Execution rules: sonnet direct vs haiku Agent
- Empty $ARGUMENTS: category menu (A/B/C/D)
- Shared Rules: path quoting, session context, TMDL tabs, DAX shell, TMSL format

From .claude/skills/pbi/commands/new.md (pattern reference for intake):
- Step 1 "Collect Requirements" pattern: parse $ARGUMENTS, extract business intent/table context/constraints
- Session context update pattern: Read-then-Write .pbi-context.md

Existing .pbi-context.md sections (must not break):
- ## Last Command
- ## Command History (20 row max)
- ## Analyst-Reported Failures (never modify)
- ## Model Context (optional, from /pbi load)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite SKILL.md to v4.0 with catch-all handler and escalation behavior</name>
  <files>.claude/skills/pbi/SKILL.md</files>
  <action>
Read the current SKILL.md. Rewrite it in place, preserving ALL existing structure (detection blocks, routing table, execution rules, shared rules) while adding the progressive friction layer. The changes are:

**1. Frontmatter:** Update `version: 4.0.0`. Keep all other frontmatter fields identical.

**2. Detection Blocks:** Keep ALL five detection blocks exactly as-is. No changes.

**3. Routing table:** Add TWO new rows:
- Add a `deep` keyword row: `deep | commands/deep.md | sonnet direct` — placed BEFORE the catch-all but AFTER existing keywords.
- Add a catch-all row as the LAST row: `(no keyword match — free-text) | Solve-first handler (inline below) | sonnet direct` — this is NOT a command file, it's handled inline in the new section below.

**4. NEW SECTION: "## Solve-First Default" (place after Routing section, before Shared Rules)**

Write this section with these exact behaviors:

```
## Solve-First Default

When no keyword matches (catch-all route), this handler runs.

### Behavior

1. **Attempt immediately.** Read the user's request as a DAX/Power BI question. Generate a solution right away — no questions, no mode announcement, no preamble. Just solve.
   - Use detection context (PBIP_MODE, File Index, Session Context) to ground the answer in the user's actual model when available.
   - If Session Context contains ## Model Context, reference actual table/column names.
   - If Session Context contains ## Escalation State with previously gathered context, use it.

2. **Deliver the answer.** Output the DAX or guidance. Let the user react.

3. **Escalation trigger.** Escalation fires ONLY when the user responds with explicit failure language:
   - Negative signals: "that's wrong", "not what I meant", "still broken", "doesn't work", "incorrect", "nope", "try again", "that's not right"
   - Normal follow-ups ("can you also...", "what about...", "and then...") are NOT failure signals — handle them as new requests.
   - Refinement requests ("make it a percentage", "add a filter for...") are NOT failure signals — just refine the answer.

4. **On failure signal — diagnose the gap.** Read the user's failure description to determine which context is missing:
   - "Calculating the wrong thing" / "not what I asked for" → missing **business question** clarity
   - "Wrong columns" / "table doesn't exist" / "relationship issue" → missing **data model state**
   - "We already have that" / "duplicates an existing measure" → missing **existing measures** knowledge
   - "Filter is wrong" / "wrong numbers in visual" → missing **visual/model context**

5. **Ask ONE targeted question.** Based on the diagnosed gap, ask exactly one question:
   - Business question gap: "What business question should this measure answer? (e.g., 'monthly revenue growth compared to prior year')"
   - Data model gap: "Can you describe the relevant tables and how they relate? (e.g., 'Sales fact table linked to Date and Product dimensions')"
   - Existing measures gap: "What existing measures are in play? (paste names or describe what's already built)"
   - Visual/model context gap: "Where will this measure be used — which visual, and what slicers or filters are active?"

   Prefix with a brief acknowledgment: "Let me get more context." Then the question. Nothing else.

6. **Write escalation state.** After asking the question, update `.pbi-context.md`:
   - Read the file with Read tool.
   - Add or update `## Escalation State` section with the question asked and "awaiting: [gap type]".
   - Write back with Write tool.

7. **On user's answer — retry automatically.** When the user answers the escalation question:
   - Incorporate the new context into the solution.
   - Retry immediately — do NOT ask "shall I try again?" or prompt for re-submission.
   - Update `## Escalation State` in `.pbi-context.md`: replace "awaiting" with the gathered answer summary.

8. **Re-escalation.** If the user signals failure AGAIN after an escalation retry:
   - Diagnose the NEXT unresolved gap (skip gaps already answered in Escalation State).
   - Ask one more targeted question about the remaining gap.
   - Same flow: acknowledge, ask, write state, retry on answer.

9. **Session context update.** After each solve attempt (initial or retry), update `.pbi-context.md`:
   - `## Last Command`: Command = `catch-all`, Request summary, Outcome
   - `## Command History`: append row, keep 20 max
   - Do NOT modify `## Analyst-Reported Failures`
```

**5. Empty $ARGUMENTS update:** Keep the existing category menu but add one line at the end of the menu, before the "Type A, B, C, or D" line:
```
**E — Deep mode**
  Full structured workflow with upfront context gathering
```
And update the routing for "E": Route to `/pbi deep`.

Update the free-text response handler at the bottom: "Free-text response: Apply the keyword matching from the Routing table above. If no keyword matches, route to **Solve-First Default** handler and treat the text as the request."

**6. Shared Rules:** Keep ALL existing shared rules. Add one new bullet:
```
- Escalation state: `## Escalation State` in `.pbi-context.md` tracks gathered context during escalation. Read before solving (use existing context), write after asking escalation questions. Clear at session start if stale.
```

**CRITICAL CONSTRAINTS:**
- Do NOT modify any existing command files (explain.md, format.md, etc.)
- Do NOT change detection block bash commands
- Do NOT change the existing keyword routing rows — only ADD the two new rows
- Do NOT change haiku Agent execution rules
- Preserve the exact formatting style of the existing file (markdown headers, table formatting, code blocks)
  </action>
  <verify>
    <automated>cd "C:/Users/DeveshD/Documents/PBI-SKILL" && grep -c "version: 4.0.0" .claude/skills/pbi/SKILL.md && grep -c "Solve-First Default" .claude/skills/pbi/SKILL.md && grep -c "Escalation State" .claude/skills/pbi/SKILL.md && grep -c "commands/deep.md" .claude/skills/pbi/SKILL.md && grep -c "catch-all" .claude/skills/pbi/SKILL.md</automated>
  </verify>
  <done>SKILL.md v4.0 contains: (1) version 4.0.0 in frontmatter, (2) deep keyword in routing table, (3) catch-all row as last routing entry, (4) complete Solve-First Default section with all 9 behaviors, (5) updated empty-args menu with option E, (6) escalation state in shared rules. All existing detection blocks, keyword rows, execution rules, and shared rules are unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Create commands/deep.md for /pbi deep intake stub</name>
  <files>.claude/skills/pbi/commands/deep.md</files>
  <action>
Create a new command file `commands/deep.md` following the exact pattern of existing command files (see commands/new.md for structure reference). This is the Phase 1 stub — entry point and upfront intake only. Full phase gates are Phase 3.

The file structure:

```markdown
# /pbi deep

> Detection context (PBIP_MODE, PBIP_FORMAT, File Index, Session Context) is provided by the router.

## Instructions

### Step 0 — Check Existing Context

Read Session Context from detection output. Check `.pbi-context.md` for existing sections:
- `## Business Question` — if present, display: "Business question on file: [content]" and ask "Still the focus, or something new?"
- `## Model Context` — if present, display: "Model context loaded from prior /pbi load."
- `## Existing Measures` — if present, display: "Existing measures on file: [summary]"

For any section that exists with content, skip asking that question in Step 1.

### Step 1 — Full Upfront Intake

Collect all three context dimensions. For each one NOT already in `.pbi-context.md`, ask the question. Ask them ONE AT A TIME (not all at once) to keep the conversation focused.

**Question 1 — Business Question:**
"What business question are we solving? (e.g., 'Compare regional sales performance month-over-month' or 'Track customer retention by cohort')"

Wait for answer. Then proceed to next missing question.

**Question 2 — Data Model State:**
"Describe your data model — key tables, how they relate, and any known issues. (e.g., 'Sales fact → Date dimension, Product dimension. No customer table yet.')"

Wait for answer. Then proceed to next missing question.

**Question 3 — Existing Measures:**
"What measures already exist that are relevant? (paste names, or say 'none yet' / 'not sure')"

Wait for answer.

### Step 2 — Write Context

Read `.pbi-context.md` with Read tool. Add or update these sections with the gathered answers:

- `## Business Question`: The stated business question, verbatim from user
- `## Existing Measures`: The user's answer about existing measures
- `## Last Command`: Command = `/pbi deep`, Timestamp = current UTC ISO 8601, Outcome = `Deep mode intake complete`
- `## Command History`: Append row, keep 20 max

Do NOT modify `## Analyst-Reported Failures`.
Do NOT modify `## Model Context` (that's managed by /pbi load).

Write the full file back with Write tool.

### Step 3 — Confirm and Ready

Output a summary of gathered context:

```
**Deep mode context gathered:**
- Business question: [summary]
- Model state: [summary or "from /pbi load"]
- Existing measures: [summary]

Context saved to .pbi-context.md. All /pbi commands will use this context going forward.

What would you like to work on first?
```

The user can now use any /pbi subcommand and the gathered context will inform the response via Session Context detection.

### Anti-Patterns
- NEVER enter deep mode unless the user explicitly typed `/pbi deep` — no automatic upgrade
- NEVER ask all 3 questions at once — ask one, wait, ask next
- NEVER re-ask a question if the answer is already in `.pbi-context.md`
- NEVER impose phase gates in Phase 1 — that's Phase 3 scope
```

If `$ARGUMENTS` contains a description after "deep" (e.g., `/pbi deep build a sales dashboard`), use it as an initial description but still run through the intake questions that aren't covered by the description.
  </action>
  <verify>
    <automated>cd "C:/Users/DeveshD/Documents/PBI-SKILL" && test -f .claude/skills/pbi/commands/deep.md && grep -c "Business Question" .claude/skills/pbi/commands/deep.md && grep -c "pbi deep" .claude/skills/pbi/commands/deep.md && grep -c "Anti-Patterns" .claude/skills/pbi/commands/deep.md</automated>
  </verify>
  <done>commands/deep.md exists with: (1) standard command file header with detection context note, (2) Step 0 checking existing context in .pbi-context.md, (3) Step 1 with 3 sequential intake questions, (4) Step 2 writing context to .pbi-context.md, (5) Step 3 confirmation summary, (6) anti-patterns section. File follows the same structural pattern as commands/new.md.</done>
</task>

</tasks>

<verification>
1. SKILL.md is valid markdown with no broken formatting
2. All existing routing keywords still present and unchanged
3. New routing rows (deep + catch-all) are in correct position
4. Solve-First Default section has all 9 numbered behaviors
5. deep.md follows the command file pattern (header, detection note, numbered steps, anti-patterns)
6. No existing command files were modified
</verification>

<success_criteria>
- `/pbi explain [measure]` still routes to commands/explain.md (existing behavior preserved)
- `/pbi deep` routes to commands/deep.md (new behavior)
- `/pbi [free text request]` triggers Solve-First Default handler (new behavior)
- Solve-First Default attempts immediately, escalates only on failure signals
- Escalation asks exactly one targeted question per blocking gap
- Deep mode asks 3 questions sequentially, skipping any already in .pbi-context.md
- Both handlers read/write .pbi-context.md following existing conventions
</success_criteria>

<output>
After completion, create `.planning/phases/01-skill-core-escalation/01-01-SUMMARY.md`
</output>
