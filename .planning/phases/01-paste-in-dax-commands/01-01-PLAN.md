---
phase: 01-paste-in-dax-commands
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/pbi-explain/SKILL.md
  - .claude/skills/pbi-format/SKILL.md
  - .claude/skills/pbi-optimise/SKILL.md
  - .claude/skills/pbi-comment/SKILL.md
  - .claude/skills/pbi-load/SKILL.md
  - .claude/skills/pbi-error/SKILL.md
  - .pbi-context.md
  - tests/fixtures/simple-measure.dax
  - tests/fixtures/intermediate-measure.dax
  - tests/fixtures/complex-measure.dax
  - tests/fixtures/slow-filter-measure.dax
  - tests/fixtures/error-log.txt
autonomous: true
requirements:
  - INFRA-01
  - CTX-01

must_haves:
  truths:
    - "All six skill directories exist under .claude/skills/"
    - "Each skill directory contains a SKILL.md file that Claude Code can discover via the / command menu"
    - ".pbi-context.md exists in the project root with the correct initial schema"
    - "Five test fixture files exist in tests/fixtures/ for use in manual verification"
    - "/pbi:load responds with a Phase 2 informational stub when invoked"
  artifacts:
    - path: ".claude/skills/pbi-explain/SKILL.md"
      provides: "Skill shell for /pbi:explain — discoverable from / menu"
    - path: ".claude/skills/pbi-format/SKILL.md"
      provides: "Skill shell for /pbi:format"
    - path: ".claude/skills/pbi-optimise/SKILL.md"
      provides: "Skill shell for /pbi:optimise"
    - path: ".claude/skills/pbi-comment/SKILL.md"
      provides: "Skill shell for /pbi:comment"
    - path: ".claude/skills/pbi-load/SKILL.md"
      provides: "Skill stub for /pbi:load (Phase 1: informational only)"
    - path: ".claude/skills/pbi-error/SKILL.md"
      provides: "Skill shell for /pbi:error"
    - path: ".pbi-context.md"
      provides: "Initial session state file with correct schema"
    - path: "tests/fixtures/simple-measure.dax"
      provides: "Simple DAX fixture for DAX-01, DAX-03 manual testing"
    - path: "tests/fixtures/intermediate-measure.dax"
      provides: "Intermediate DAX fixture for DAX-02, DAX-03 manual testing"
    - path: "tests/fixtures/complex-measure.dax"
      provides: "Complex DAX fixture for DAX-09 manual testing"
    - path: "tests/fixtures/slow-filter-measure.dax"
      provides: "Slow-pattern DAX fixture for DAX-08 manual testing"
    - path: "tests/fixtures/error-log.txt"
      provides: "Sample PBI error log for ERR-01 manual testing"
  key_links:
    - from: ".claude/skills/pbi-*/SKILL.md"
      to: "Claude Code / command menu"
      via: "SKILL.md name: frontmatter field"
      pattern: "^name: pbi-"
    - from: "All DAX skills"
      to: ".pbi-context.md"
      via: "! bash injection reads file at startup"
      pattern: "cat .pbi-context.md 2>/dev/null"
---

<objective>
Create the skill directory structure, initial `.pbi-context.md` session file, the `/pbi:load` Phase 1 stub, and all test fixtures.

Purpose: Every subsequent plan in this phase writes its SKILL.md into a directory that must already exist, and reads `.pbi-context.md` which must already exist with the correct schema. This plan establishes that foundation.
Output: Six skill directories with placeholder/stub SKILL.md files; `.pbi-context.md`; five test fixtures.
</objective>

<execution_context>
@C:/Users/DeveshD/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/DeveshD/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-paste-in-dax-commands/01-CONTEXT.md
@.planning/phases/01-paste-in-dax-commands/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create skill directories, .pbi-context.md, and /pbi:load stub</name>
  <files>
    .claude/skills/pbi-explain/SKILL.md
    .claude/skills/pbi-format/SKILL.md
    .claude/skills/pbi-optimise/SKILL.md
    .claude/skills/pbi-comment/SKILL.md
    .claude/skills/pbi-load/SKILL.md
    .claude/skills/pbi-error/SKILL.md
    .pbi-context.md
  </files>
  <action>
Create the six skill directories and write each SKILL.md. For pbi-explain, pbi-format, pbi-optimise, pbi-comment, and pbi-error: write a placeholder SKILL.md with correct frontmatter and a single line body — "Instructions pending — see Wave 1 plans." These are shells that make the commands discoverable now; full content arrives in plans 02–06.

For pbi-load, write the complete Phase 1 stub (this is its final content for Phase 1):

```
---
name: pbi-load
description: Load PBIP model context for use by DAX commands. Use when the analyst asks to load model context or when a PBIP project is present.
disable-model-invocation: true
model: haiku
allowed-tools: Read
---

Model context loading is available when a PBIP project is present (Phase 2).

For now, paste your DAX measure directly into any `/pbi` command — all DAX commands work without prior model loading.

**Available commands:**
- `/pbi:explain` — explain a DAX measure
- `/pbi:format` — format a DAX measure to SQLBI style
- `/pbi:optimise` — optimise a DAX measure with rationale
- `/pbi:comment` — add inline comments and generate a Description field value
- `/pbi:error` — diagnose a Power BI error
```

Frontmatter fields to use on ALL skills (placeholder and stub alike):
- `disable-model-invocation: true` — analyst must deliberately invoke; Claude must not auto-trigger
- `model: sonnet` for all reasoning skills (explain, format, optimise, comment, error); `model: haiku` for pbi-load
- `allowed-tools: Read, Write` as the base for placeholder skills

Then write `.pbi-context.md` in the project root with this exact initial schema:

```markdown
# PBI Context

## Last Command
- Command: (none)
- Timestamp: (none)
- Measure: (none)
- Outcome: (none)

## Command History
| Timestamp | Command | Measure Name | Outcome |
|-----------|---------|--------------|---------|

## Analyst-Reported Failures
| Timestamp | Command | Measure Name | What Failed | Notes |
|-----------|---------|--------------|-------------|-------|
```

Do NOT use absolute paths in any skill's bash injection calls — always use relative paths like `.pbi-context.md`.
  </action>
  <verify>
    <automated>ls .claude/skills/pbi-explain/SKILL.md .claude/skills/pbi-format/SKILL.md .claude/skills/pbi-optimise/SKILL.md .claude/skills/pbi-comment/SKILL.md .claude/skills/pbi-load/SKILL.md .claude/skills/pbi-error/SKILL.md .pbi-context.md</automated>
  </verify>
  <done>All six SKILL.md files exist in their directories. .pbi-context.md exists with the three-section schema. All skill frontmatter has disable-model-invocation: true.</done>
</task>

<task type="auto">
  <name>Task 2: Create test fixtures</name>
  <files>
    tests/fixtures/simple-measure.dax
    tests/fixtures/intermediate-measure.dax
    tests/fixtures/complex-measure.dax
    tests/fixtures/slow-filter-measure.dax
    tests/fixtures/error-log.txt
  </files>
  <action>
Create the tests/fixtures/ directory and five fixture files. Each file contains the exact content shown below — these are the canonical test inputs for manual verification throughout Phase 1.

tests/fixtures/simple-measure.dax:
```
Revenue = SUM(Sales[Amount])
```

tests/fixtures/intermediate-measure.dax:
```
Sales YTD = CALCULATE([Revenue], DATESYTD('Date'[Date]))
```

tests/fixtures/complex-measure.dax:
```
Risky Revenue = SUMX(Customers, [Revenue])
```
(This measure iterates over a measure reference — covers DAX-09 iterator guard.)

tests/fixtures/slow-filter-measure.dax:
```
Slow Sales 2024 = CALCULATE(SUM(Sales[Amount]), FILTER(Sales, Sales[Year] = 2024))
```
(FILTER on an entire table — covers DAX-08 Rule 1.)

tests/fixtures/error-log.txt:
```
Error in expression: The name 'Revenue' does not exist in the current context.

Location: Measure 'Sales YTD' in table 'Sales'
Expression: CALCULATE([Revenue], DATESYTD('Date'[Date]))

Details: The referenced measure [Revenue] cannot be resolved. Verify that the measure exists and is in scope.
```
  </action>
  <verify>
    <automated>ls tests/fixtures/simple-measure.dax tests/fixtures/intermediate-measure.dax tests/fixtures/complex-measure.dax tests/fixtures/slow-filter-measure.dax tests/fixtures/error-log.txt</automated>
  </verify>
  <done>Five fixture files exist in tests/fixtures/. Each file contains non-empty DAX content or error text matching the specification above.</done>
</task>

</tasks>

<verification>
Run after both tasks complete:

```bash
ls .claude/skills/pbi-explain/SKILL.md \
   .claude/skills/pbi-format/SKILL.md \
   .claude/skills/pbi-optimise/SKILL.md \
   .claude/skills/pbi-comment/SKILL.md \
   .claude/skills/pbi-load/SKILL.md \
   .claude/skills/pbi-error/SKILL.md \
   .pbi-context.md \
   tests/fixtures/simple-measure.dax \
   tests/fixtures/intermediate-measure.dax \
   tests/fixtures/complex-measure.dax \
   tests/fixtures/slow-filter-measure.dax \
   tests/fixtures/error-log.txt
```

All 12 files must be listed without error.

Verify pbi-load has the informational response (not just a placeholder):
```bash
grep -c "paste your DAX" .claude/skills/pbi-load/SKILL.md
```
Should return 1.

Verify .pbi-context.md has correct schema sections:
```bash
grep "## Last Command\|## Command History\|## Analyst-Reported Failures" .pbi-context.md
```
Should return all three headers.
</verification>

<success_criteria>
- 6 skill directories under .claude/skills/ each containing a SKILL.md
- .pbi-context.md in project root with three-section initial schema
- 5 test fixture files in tests/fixtures/
- /pbi:load stub is complete (informs analyst that Phase 2 enables PBIP reading)
- All SKILL.md frontmatter includes disable-model-invocation: true
</success_criteria>

<output>
After completion, create `.planning/phases/01-paste-in-dax-commands/01-01-SUMMARY.md`
</output>
