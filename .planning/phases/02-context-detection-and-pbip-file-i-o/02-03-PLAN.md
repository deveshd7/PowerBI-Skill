---
phase: 02-context-detection-and-pbip-file-i-o
plan: 03
type: execute
wave: 2
depends_on:
  - "02-01"
  - "02-02"
autonomous: false
requirements:
  - INFRA-03
  - INFRA-06
  - DAX-13

files_modified:
  - .claude/skills/pbi-comment/SKILL.md

must_haves:
  truths:
    - "When no .SemanticModel/ folder is present, /pbi:comment produces paste-in output with no mention of file mode"
    - "When a PBIP project is detected, the output begins with a file-mode header showing format and Desktop status"
    - "When Desktop is closed and PBIP is present, the skill writes the commented DAX and description back to the correct .tmdl or model.bim file"
    - "When Desktop is open, the skill delivers paste-ready output only with the note 'Desktop is open — paste manually, then save'"
    - "If the measure name is not found in any PBIP file, the skill reports the not-found message and delivers paste-ready output"
    - "All existing paste-in behavior (Steps 1-7) is unchanged when in paste-in mode"
  artifacts:
    - path: ".claude/skills/pbi-comment/SKILL.md"
      provides: "pbi-comment with file-mode branch prepended before existing Step 1"
      contains: "PBIP_MODE"
  key_links:
    - from: ".claude/skills/pbi-comment/SKILL.md"
      to: ".SemanticModel/definition/tables/*.tmdl"
      via: "grep -rl for measure name, then Read tool, then Write tool"
      pattern: "grep -rl"
    - from: ".claude/skills/pbi-comment/SKILL.md"
      to: ".SemanticModel/model.bim"
      via: "Read tool (full file), locate measure object, update description + expression, Write tool"
      pattern: "model.bim"
---

<objective>
Add a file-mode branch to pbi-comment/SKILL.md. When a PBIP project is detected and Desktop is confirmed closed, the skill locates the target measure in TMDL or TMSL files, performs the existing commenting logic, and writes the result back to disk. Paste-in mode behavior is completely unchanged.

Purpose: Satisfies DAX-13 and the Desktop safety guard (INFRA-06) for the comment command. Analysts with PBIP projects no longer need to manually copy-paste commented DAX back into their files.
Output: pbi-comment/SKILL.md with a file-mode detection header and write-back branch that activates only when appropriate.
</objective>

<execution_context>
@C:/Users/DeveshD/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/DeveshD/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-context-detection-and-pbip-file-i-o/02-RESEARCH.md
@.planning/phases/02-context-detection-and-pbip-file-i-o/02-CONTEXT.md

<interfaces>
<!-- Current pbi-comment/SKILL.md structure (must be preserved for paste-in mode) -->

Current Step flow:
  Step 1 — Initial Response: "Paste your DAX measure below:" (wait for paste)
  Step 2 — Measure Extraction: extract measure name from text before first =
  Step 3 — Prior Failure Check: scan .pbi-context.md Analyst-Reported Failures
  Step 4 — Comment Placement Rules: // inline comment rules
  Step 5 — Description Field Generation Rules: 300-char plain-text description
  Step 6 — Output Structure: Commented DAX block + Description Field block
  Step 7 — Context Update: Read-then-Write .pbi-context.md

Locked decisions from CONTEXT.md:
  - No --force flag. If Desktop is open: paste-ready output only, always.
  - Silent fallback to paste-in if no PBIP project. No mention of file mode.
  - File mode header at top: "File mode — PBIP project detected (TMDL) | Desktop: closed — will write to disk"
    OR "File mode — PBIP project detected (TMDL) | Desktop: open — output is paste-ready"
  - pbi-comment writes WITHOUT a confirm prompt (only pbi-error requires confirm)
  - Post-write confirmation line: "Written to: [MeasureName] in [file path]"
  - Measure not found: "Measure [Name] not found in PBIP project — output is paste-ready for manual addition."
  - Duplicate measure names: "Measure [Name] found in multiple tables: [Table1], [Table2]. Use --table TableName to specify."

TMDL write-back pattern (from RESEARCH.md):
  1. grep -rl "measure.*MeasureName" .SemanticModel/definition/tables/
  2. Read the matching .tmdl file
  3. Locate the measure block (optional /// line + measure keyword line + expression + properties)
  4. Replace/insert /// description immediately above measure keyword (no blank line)
  5. Replace expression body with commented DAX (preserve tab indentation)
  6. Write entire file back

TMSL write-back pattern (from RESEARCH.md):
  1. Read .SemanticModel/model.bim
  2. Locate measure object where "name" == MeasureName
  3. Update "description" field (string)
  4. Update "expression" field — preserve string vs array form; use array form if inline comments add line breaks
  5. Preserve ALL other fields (formatString, displayFolder, annotations)
  6. Write entire model.bim back
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prepend startup detection block to pbi-comment</name>
  <files>.claude/skills/pbi-comment/SKILL.md</files>
  <action>
Read the current pbi-comment/SKILL.md. Prepend the following three bash injection blocks at the top of the skill body (after the YAML frontmatter, before the existing `## Session Context` block).

**Important:** Do NOT replace the existing `## Session Context` injection — it stays where it is. The new blocks go BEFORE it.

Block 1 — PBIP detection (same pattern as pbi-load, established in RESEARCH.md):
```yaml
## PBIP Detection
!`PBIP_RESULT=""; if [ -d ".SemanticModel" ]; then PBISM=$(cat ".SemanticModel/definition.pbism" 2>/dev/null); if echo "$PBISM" | grep -q '"version": "1.0"'; then PBIP_RESULT="PBIP_MODE=file PBIP_FORMAT=tmsl"; else PBIP_RESULT="PBIP_MODE=file PBIP_FORMAT=tmdl"; fi; else PBIP_RESULT="PBIP_MODE=paste"; fi; echo "$PBIP_RESULT"`
```

Block 2 — Desktop safety check:
```yaml
## Desktop Check
!`tasklist /fi "imagename eq PBIDesktop.exe" 2>/dev/null | findstr /i "PBIDesktop.exe" >nul 2>&1 && echo "DESKTOP=open" || echo "DESKTOP=closed"`
```

The existing Session Context block stays in place after these two new blocks:
```yaml
## Session Context
!`cat .pbi-context.md 2>/dev/null | tail -80 || echo "No prior context found."`
```

**Add a new "File Mode Branch" section immediately after the Session Context block and BEFORE Step 1 — Initial Response:**

```
## File Mode Branch

If the PBIP Detection output contains `PBIP_MODE=paste`:
- Proceed directly to Step 1 (paste-in mode). Do not output any file-mode header. Do not mention PBIP at all.

If the PBIP Detection output contains `PBIP_MODE=file`:
1. Output this header as the first line of your response (before Step 1 prompt):
   > File mode — PBIP project detected ([FORMAT]) | Desktop: [STATUS]
   Where [FORMAT] is "TMDL" if PBIP_FORMAT=tmdl, or "TMSL (model.bim)" if PBIP_FORMAT=tmsl.
   Where [STATUS] is "closed — will write to disk" if DESKTOP=closed, or "open — output is paste-ready" if DESKTOP=open.

2. Proceed to Step 1 (paste-in flow) to collect the measure and generate commented output. The measure name extracted in Step 2 and the output from Steps 4-5 will be used for write-back.

3. After completing Steps 2-6 (output generated), check DESKTOP status:
   - DESKTOP=open: add the note "Desktop is open — paste manually, then save." below the Description Field. Do not write any files. Skip to Step 7 (context update).
   - DESKTOP=closed: proceed to File Write-Back (see below).

### File Write-Back (DESKTOP=closed, PBIP_MODE=file)

Use the measure name extracted in Step 2 as the search key.

**If PBIP_FORMAT=tmdl:**
1. Run bash: `grep -rl "measure.*[MeasureName]" ".SemanticModel/definition/tables/" 2>/dev/null`
   - Replace [MeasureName] with the actual extracted measure name.
   - If multiple files returned: output "Measure [Name] found in multiple tables: [list]. Use --table TableName to specify which one." Deliver paste-ready output only. Stop write-back.
   - If no file returned: output "Measure [Name] not found in PBIP project — output is paste-ready for manual addition." Stop write-back.
   - If exactly one file returned: proceed.
2. Read the identified .tmdl file using the Read tool.
3. Locate the measure block:
   - Find the line matching `measure.*[MeasureName]` (the measure declaration line)
   - The line(s) immediately above starting with `///` are the existing description (may be absent)
   - The lines following the measure declaration through the blank line or next `measure`/`column`/`table` keyword are the expression and properties
4. Modify the block:
   - Replace or insert `///` description line directly above the measure declaration line (no blank line between `///` and `measure`). Use the Description Field value from Step 5 as the description text.
   - Replace the expression body with the commented DAX from Step 4. Preserve tab indentation (TMDL uses tabs — do NOT convert to spaces). Preserve formatString, displayFolder, and any other property lines that follow the expression.
5. Write the entire modified .tmdl file back using the Write tool.
6. Append the write confirmation line after the Description Field in the output:
   > Written to: [MeasureName] in [file path]

**If PBIP_FORMAT=tmsl:**
1. Read `.SemanticModel/model.bim` using the Read tool.
2. Locate the measure JSON object where `"name"` equals the extracted measure name.
   - If not found: output "Measure [Name] not found in PBIP project — output is paste-ready for manual addition." Stop write-back.
3. Update the measure object:
   - Set `"description"` to the Description Field value from Step 5 (plain string).
   - Update `"expression"` with the commented DAX from Step 4. CRITICAL: detect whether the original expression was a JSON string or a JSON array of strings. If the commented DAX has line breaks (which it will if // comments were added inline), use the array form. Preserve the exact array/string form of the original if the expression is unchanged; use array if adding comments creates new lines.
   - Preserve ALL other fields: formatString, displayFolder, annotations, etc.
4. Write the entire model.bim back using the Write tool.
5. Append the write confirmation line after the Description Field in the output:
   > Written to: [MeasureName] in .SemanticModel/model.bim

Edge cases (Claude's discretion per CONTEXT.md):
- `tasklist` produces a permission error or empty output: treat as DESKTOP=closed and proceed with write. Log note but do not block.
- .tmdl file has unusual indentation: preserve whatever indentation style is already in the file.
```
  </action>
  <verify>
    <automated>grep -q "PBIP_MODE" .claude/skills/pbi-comment/SKILL.md && grep -q "DESKTOP" .claude/skills/pbi-comment/SKILL.md && grep -q "File Write-Back" .claude/skills/pbi-comment/SKILL.md && grep -q "model.bim" .claude/skills/pbi-comment/SKILL.md && echo "File-mode branch present (TMDL + TMSL paths)" || echo "MISSING"</automated>
  </verify>
  <done>
    - SKILL.md has PBIP Detection bash injection block at top of body
    - SKILL.md has Desktop Check bash injection block
    - SKILL.md has File Mode Branch section before Step 1
    - File Mode Branch covers: paste-in mode (silent pass-through), file-mode header, Desktop=open abort path, Desktop=closed write-back
    - TMDL write-back path documented: grep locate → Read → modify block → Write
    - TMSL write-back path documented: Read model.bim → locate measure → update description + expression → Write
    - All existing Step 1-7 instructions remain unchanged
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual verification of pbi-comment file-mode paths</name>
  <what-built>
    pbi-comment/SKILL.md with a file-mode detection header and write-back branch for TMDL and TMSL projects. The skill detects PBIP presence, checks Desktop status, generates commented DAX, and writes back to the correct file when Desktop is closed.
  </what-built>
  <how-to-verify>
Run these tests using the fixtures from Plan 01 (tests/fixtures/pbip-tmdl/ and tests/fixtures/pbip-tmsl/):

**Test A — Paste-in mode (no PBIP project):**
1. cd to any directory WITHOUT a .SemanticModel/ folder
2. Run /pbi:comment, paste: `Revenue = SUM(Sales[Amount])`
3. Expected: No file-mode header. Normal paste-in output (Commented DAX + Description Field).

**Test B — File mode, TMDL, Desktop closed:**
1. cd tests/fixtures/pbip-tmdl
2. Confirm PBIDesktop.exe is NOT running (check Task Manager)
3. Run /pbi:comment, paste: `Revenue YTD = CALCULATE([Revenue], DATESYTD('Date'[Date]))`
4. Expected:
   - Header: "File mode — PBIP project detected (TMDL) | Desktop: closed — will write to disk"
   - Normal commented output for the measure
   - Final line: "Written to: Revenue YTD in .SemanticModel/definition/tables/Sales.tmdl"
5. Verify: open Sales.tmdl — measure should have updated /// description and // inline comments

**Test C — File mode, TMDL, Desktop open:**
1. Start PBIDesktop.exe (or rename any exe to PBIDesktop.exe temporarily for testing)
2. Run /pbi:comment with the same measure
3. Expected:
   - Header: "File mode — PBIP project detected (TMDL) | Desktop: open — output is paste-ready"
   - Normal commented output
   - Note: "Desktop is open — paste manually, then save."
   - NO file written (verify Sales.tmdl is unchanged)

**Test D — TMSL format (Desktop closed):**
1. cd tests/fixtures/pbip-tmsl
2. Run /pbi:comment, paste: `Revenue = SUM(Sales[Amount])`
3. Expected: "Written to: Revenue in .SemanticModel/model.bim"
4. Verify: open model.bim — Revenue measure should have updated "description" and "expression" fields, all other fields preserved

**Test E — Measure not found:**
1. cd tests/fixtures/pbip-tmdl
2. Run /pbi:comment, paste: `NonExistentMeasure = 1`
3. Expected: "Measure NonExistentMeasure not found in PBIP project — output is paste-ready for manual addition."
  </how-to-verify>
  <resume-signal>Type "approved" if all 5 tests pass, or describe which tests failed and what was observed</resume-signal>
</task>

</tasks>

<verification>
Automated pre-verification:
```bash
# Detection blocks present
grep -c "PBIP_MODE\|DESKTOP\|File Write-Back\|model.bim\|tmdl" .claude/skills/pbi-comment/SKILL.md

# Original step structure preserved
grep -c "Step 1\|Step 2\|Step 3\|Step 4\|Step 5\|Step 6\|Step 7" .claude/skills/pbi-comment/SKILL.md
# Expected: 7
```

Manual: See Task 2 checkpoint above.
</verification>

<success_criteria>
- pbi-comment detects PBIP presence and announces file mode in the header (INFRA-03, INFRA-04)
- Desktop open → paste-ready output only, no file write (INFRA-06)
- Desktop closed + TMDL → writes /// description and // comments to .tmdl file (DAX-13)
- Desktop closed + TMSL → updates description and expression in model.bim JSON (DAX-13)
- Measure not found → clear message, paste-ready output (locked decision)
- Paste-in mode → completely unchanged behavior, no file-mode mention
</success_criteria>

<output>
After completion, create .planning/phases/02-context-detection-and-pbip-file-i-o/02-03-SUMMARY.md
</output>
