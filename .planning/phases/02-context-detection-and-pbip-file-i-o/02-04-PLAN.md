---
phase: 02-context-detection-and-pbip-file-i-o
plan: 04
type: execute
wave: 3
depends_on:
  - "02-01"
  - "02-03"
autonomous: false
requirements:
  - INFRA-03
  - INFRA-06
  - ERR-03

files_modified:
  - .claude/skills/pbi-error/SKILL.md

must_haves:
  truths:
    - "When no .SemanticModel/ folder is present, /pbi:error produces paste-in output with no mention of file mode"
    - "When a PBIP project is detected, the output begins with a file-mode header showing format and Desktop status"
    - "When Desktop is closed and PBIP is present, the skill shows a before/after preview of the proposed fix and asks 'Apply this fix? (y/N)' before writing"
    - "On 'y' confirmation, the fix is written to the correct .tmdl or model.bim file and 'Written to:' confirmation line is shown"
    - "When Desktop is open, the skill delivers paste-ready output only with the 'Desktop is open' note — no write, no confirm prompt"
    - "All existing paste-in error diagnosis behavior (Steps 1-6) is unchanged"
  artifacts:
    - path: ".claude/skills/pbi-error/SKILL.md"
      provides: "pbi-error with file-mode branch and confirm-before-write flow"
      contains: "PBIP_MODE"
  key_links:
    - from: ".claude/skills/pbi-error/SKILL.md"
      to: ".SemanticModel/definition/tables/*.tmdl"
      via: "grep locate → Read → show before/after → confirm → Write"
      pattern: "Apply this fix"
    - from: ".claude/skills/pbi-error/SKILL.md"
      to: ".SemanticModel/model.bim"
      via: "Read → locate measure → show before/after → confirm → Write"
      pattern: "model.bim"
---

<objective>
Add a file-mode branch to pbi-error/SKILL.md. When a PBIP project is detected and Desktop is confirmed closed, the skill diagnoses the error (existing behavior), proposes a specific fix as a before/after preview, and — on explicit analyst confirmation — writes the corrected expression to the target .tmdl or model.bim file.

Purpose: Satisfies ERR-03. Error recovery without file write-back requires the analyst to manually apply every fix — in file mode with Desktop closed, this is unnecessary friction that undermines the value of having a PBIP project.
Output: pbi-error/SKILL.md with a file-mode detection header and a confirm-before-write branch.
</objective>

<execution_context>
@C:/Users/DeveshD/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/DeveshD/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-context-detection-and-pbip-file-i-o/02-RESEARCH.md
@.planning/phases/02-context-detection-and-pbip-file-i-o/02-CONTEXT.md
@.planning/phases/02-context-detection-and-pbip-file-i-o/02-03-SUMMARY.md

<interfaces>
<!-- Current pbi-error/SKILL.md structure (must be preserved for paste-in mode) -->

Current Step flow:
  Step 1 — Prompt for input: "Paste your Power BI error message or error log below:"
  Step 2 — Prior error check (ERR-04): scan Analyst-Reported Failures
  Step 3 — Last-command correlation (ERR-02): read ## Last Command section
  Step 4 — Classify and diagnose: Category A-F classification with specific fix steps
  Step 5 — Output structure: Error Diagnosis + Root Cause + Fix + Verification
  Step 6 — Update .pbi-context.md: Read-then-Write

Locked decisions from CONTEXT.md:
  - pbi-error requires explicit "y" confirmation before writing (unlike pbi-comment which writes without confirm)
  - Preview required: show before/after of affected lines before asking for confirmation
  - Confirm prompt: "Apply this fix? (y/N)" — capital N = default No
  - No --force flag. Desktop open = paste-ready output only.
  - Same file-mode header as pbi-comment: "File mode — PBIP project detected (TMDL) | Desktop: closed — will write to disk"
  - Post-write confirmation: "Written to: [fix description] in [file path]"

Key difference from pbi-comment:
  - pbi-comment writes after generating output (no confirm)
  - pbi-error generates output, then proposes file fix, shows preview, asks confirm, then writes (with confirm)
  - The fix in pbi-error targets the ERROR'S affected expression — not necessarily the same as what a comment write does

TMDL and TMSL write-back mechanics:
  - Same as Plan 03: grep-locate for TMDL, Read full model.bim for TMSL
  - For pbi-error: fix replaces the expression body only (not description//// line)
  - Preserve all other measure properties (formatString, displayFolder, annotations)
  - TMSL expression array form must be preserved if original was array
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prepend startup detection block to pbi-error and add file-mode branch</name>
  <files>.claude/skills/pbi-error/SKILL.md</files>
  <action>
Read the current pbi-error/SKILL.md. Prepend the following bash injection blocks at the top of the skill body (after the YAML frontmatter, before the existing `## Session Context` block).

**Block 1 — PBIP detection (identical to pbi-comment pattern):**
```yaml
## PBIP Detection
!`PBIP_RESULT=""; if [ -d ".SemanticModel" ]; then PBISM=$(cat ".SemanticModel/definition.pbism" 2>/dev/null); if echo "$PBISM" | grep -q '"version": "1.0"'; then PBIP_RESULT="PBIP_MODE=file PBIP_FORMAT=tmsl"; else PBIP_RESULT="PBIP_MODE=file PBIP_FORMAT=tmdl"; fi; else PBIP_RESULT="PBIP_MODE=paste"; fi; echo "$PBIP_RESULT"`
```

**Block 2 — Desktop safety check:**
```yaml
## Desktop Check
!`tasklist /fi "imagename eq PBIDesktop.exe" 2>/dev/null | findstr /i "PBIDesktop.exe" >nul 2>&1 && echo "DESKTOP=open" || echo "DESKTOP=closed"`
```

The existing Session Context block stays in place after these two new blocks:
```yaml
## Session Context
!`cat .pbi-context.md 2>/dev/null | tail -100 || echo "No prior context found."`
```

Note: pbi-error uses `tail -100` (not tail -80) — this is a Phase 1 locked decision. Do not change it.

**Add a "File Mode Branch" section immediately after the Session Context block and BEFORE the existing Step 1:**

```
## File Mode Branch

If the PBIP Detection output contains `PBIP_MODE=paste`:
- Proceed directly to Step 1 (paste-in mode). Do not output any file-mode header. Do not mention PBIP at all.

If the PBIP Detection output contains `PBIP_MODE=file`:
1. Output this header as the first line of your response (before Step 1 prompt):
   > File mode — PBIP project detected ([FORMAT]) | Desktop: [STATUS]
   Where [FORMAT] is "TMDL" if PBIP_FORMAT=tmdl, or "TMSL (model.bim)" if PBIP_FORMAT=tmsl.
   Where [STATUS] is "closed — will write to disk" if DESKTOP=closed, or "open — output is paste-ready" if DESKTOP=open.

2. Proceed through Steps 1-5 (collect error, diagnose, produce output). The Fix section in Step 5 output should identify the specific measure or expression to be corrected if one can be inferred from the error.

3. After completing Step 5 output, check DESKTOP status:
   - DESKTOP=open: add the note "Desktop is open — paste manually, then save." Proceed to Step 6 (context update). Do not offer a write.
   - DESKTOP=closed: proceed to File Fix Preview (see below).

### File Fix Preview and Confirm (DESKTOP=closed, PBIP_MODE=file)

This section triggers only when:
- The error diagnosis in Step 4 identifies a specific measure or expression to fix AND
- The fix in Step 5 proposes a concrete DAX change (not just a general recommendation)

If the fix is general (no specific file target identifiable — e.g. Category D data refresh errors, Category E relationship errors, Category F unknown errors): skip file write entirely. Add after Step 5 output:
> File mode active but fix cannot be applied automatically — [brief reason, e.g. "relationship errors require changes in Desktop's Model view"]. Output above is paste-ready.

If the fix targets a specific measure expression (Category A name errors with a rewrite, Category B circular dependency fix involving a specific measure, Category C context transition fix):

1. Ask the analyst to confirm the measure name:
   > Fix targets measure: [inferred measure name] — is this correct? (y/N)
   Wait for confirmation before locating the file.

   Note: Only ask this confirmation if the measure name was not explicitly provided in the error text. If the error clearly names the failing measure (e.g. "The name '[Revenue YTD]' does not exist"), use that name directly without asking — proceed straight to step 2.

2. Locate the measure file:
   **TMDL:** `grep -rl "measure.*[MeasureName]" ".SemanticModel/definition/tables/" 2>/dev/null`
   - Multiple matches: "Measure found in multiple tables: [list]. Which table? Type the table name."
   - No match: "Measure [Name] not found in PBIP project — fix is paste-ready for manual application." Stop.
   - One match: proceed.
   **TMSL:** Read `.SemanticModel/model.bim`. Locate measure object by name. If not found: same not-found message.

3. Read the identified file (Read tool).

4. Show before/after preview:
   > **Before:**
   > ```
   > [current expression — the relevant lines from the measure block]
   > ```
   > **After:**
   > ```
   > [proposed corrected expression from Step 5 Fix]
   > ```
   >
   > Apply this fix? (y/N)

   Wait for the analyst's response.
   - "n", "N", or anything other than "y"/"Y": "Fix not applied. Output above is paste-ready." Proceed to Step 6.
   - "y" or "Y": proceed to write.

5. Write the fix:
   **TMDL:** Replace the expression body in the measure block. Preserve tab indentation, formatString, displayFolder, and all properties. Only modify the expression lines. Write the entire .tmdl file back.
   **TMSL:** Update `"expression"` field only (string or array form — preserve original form; use array if fix creates line breaks). Preserve all other fields. Write entire model.bim back.

6. Append write confirmation after the preview:
   > Written to: [MeasureName] expression in [file path]

7. Proceed to Step 6 (context update).
```
  </action>
  <verify>
    <automated>grep -q "PBIP_MODE" .claude/skills/pbi-error/SKILL.md && grep -q "Apply this fix" .claude/skills/pbi-error/SKILL.md && grep -q "tail -100" .claude/skills/pbi-error/SKILL.md && echo "File-mode branch present, tail-100 preserved" || echo "MISSING"</automated>
  </verify>
  <done>
    - SKILL.md has PBIP Detection and Desktop Check bash injection blocks
    - SKILL.md has File Mode Branch section with paste/file routing
    - File mode: header output, Desktop=open abort path, Desktop=closed fix-preview path
    - Fix preview: before/after diff shown, "Apply this fix? (y/N)" prompt
    - TMDL and TMSL write-back documented (expression-only update, preserve other fields)
    - tail -100 unchanged (Phase 1 locked decision)
    - All existing Step 1-6 instructions unchanged
    - Measure name confirmation step included (only when name not clearly identifiable from error text)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual verification of pbi-error file-mode paths</name>
  <what-built>
    pbi-error/SKILL.md with file-mode detection, confirm-before-write fix application for TMDL and TMSL projects, and Desktop safety guard.
  </what-built>
  <how-to-verify>
Run these tests using the fixtures from Plan 01 (tests/fixtures/pbip-tmdl/ and tests/fixtures/pbip-tmsl/):

**Test A — Paste-in mode (no PBIP project):**
1. cd to any directory WITHOUT a .SemanticModel/ folder
2. Run /pbi:error, paste any Power BI error message
3. Expected: No file-mode header. Normal paste-in error diagnosis output.

**Test B — File mode, TMDL, Desktop closed, Category A error (measure name):**
1. cd tests/fixtures/pbip-tmdl
2. Confirm PBIDesktop.exe is NOT running
3. Run /pbi:error, paste: "The name 'Revenue YTD' does not exist in the current context."
4. Expected:
   - Header: "File mode — PBIP project detected (TMDL) | Desktop: closed — will write to disk"
   - Category A diagnosis output (Root Cause + Fix)
   - Before/after preview of the measure's expression
   - Prompt: "Apply this fix? (y/N)"
5. Type "y" and verify:
   - "Written to: Revenue YTD expression in .SemanticModel/definition/tables/Sales.tmdl"
   - Open Sales.tmdl and verify the expression was updated

**Test C — File mode, confirm rejected:**
1. Same setup as Test B
2. At "Apply this fix? (y/N)" prompt, type "n"
3. Expected: "Fix not applied. Output above is paste-ready." No file change.

**Test D — Desktop open:**
1. Start PBIDesktop.exe (or simulate)
2. Run /pbi:error in TMDL fixture directory
3. Expected:
   - Header: "File mode — PBIP project detected (TMDL) | Desktop: open — output is paste-ready"
   - Normal diagnosis
   - Note: "Desktop is open — paste manually, then save."
   - NO confirm prompt shown, NO file written

**Test E — Non-fixable error (Category E relationship error):**
1. cd tests/fixtures/pbip-tmdl (Desktop closed)
2. Run /pbi:error, paste a relationship ambiguity error
3. Expected: "File mode active but fix cannot be applied automatically — relationship errors require changes in Desktop's Model view."
  </how-to-verify>
  <resume-signal>Type "approved" if all 5 tests pass, or describe which tests failed and what was observed</resume-signal>
</task>

</tasks>

<verification>
Automated pre-verification:
```bash
# Detection blocks present
grep -c "PBIP_MODE\|DESKTOP\|Apply this fix\|Before:\|After:" .claude/skills/pbi-error/SKILL.md

# tail -100 preserved (Phase 1 locked decision)
grep "tail -100" .claude/skills/pbi-error/SKILL.md && echo "OK" || echo "ERROR: tail-100 changed"

# Original step structure preserved
grep -c "Step 1\|Step 2\|Step 3\|Step 4\|Step 5\|Step 6" .claude/skills/pbi-error/SKILL.md
# Expected: 6
```

Manual: See Task 2 checkpoint above.
</verification>

<success_criteria>
- pbi-error detects PBIP presence and announces file mode in the header (INFRA-03, INFRA-04)
- Desktop open → paste-ready output only, no write, no confirm prompt (INFRA-06)
- Desktop closed + fixable measure error → before/after preview + "Apply this fix? (y/N)" (ERR-03)
- On "y": fix written to correct .tmdl or model.bim file, "Written to:" confirmation shown
- On "n" or non-fixable error: graceful fallback to paste-ready output
- Paste-in mode: completely unchanged behavior
- tail -100 session context injection preserved
</success_criteria>

<output>
After completion, create .planning/phases/02-context-detection-and-pbip-file-i-o/02-04-SUMMARY.md
</output>
