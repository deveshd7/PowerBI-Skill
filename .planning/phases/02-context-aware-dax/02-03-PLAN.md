---
phase: 02-context-aware-dax
plan: 03
type: execute
wave: 1
depends_on: [02-01]
files_modified:
  - .claude/skills/pbi/commands/explain.md
  - .claude/skills/pbi/commands/format.md
  - .claude/skills/pbi/commands/optimise.md
autonomous: true
requirements: [DAX-01]

must_haves:
  truths:
    - "Running /pbi explain with empty .pbi-context.md causes the skill to ask which table the measure belongs to"
    - "Running /pbi format with empty .pbi-context.md causes the skill to ask which table (lightweight — does not block formatting)"
    - "Running /pbi optimise with empty .pbi-context.md causes the skill to ask which table and related tables"
    - "If Model Context is already present, all three commands skip the context question"
  artifacts:
    - path: ".claude/skills/pbi/commands/explain.md"
      provides: "Updated explain with Step 0.5 (model context check)"
      contains: "Step 0.5"
    - path: ".claude/skills/pbi/commands/format.md"
      provides: "Updated format with Step 0.5 (model context check, lightweight)"
      contains: "Step 0.5"
    - path: ".claude/skills/pbi/commands/optimise.md"
      provides: "Updated optimise with Step 0.5 (model context check)"
      contains: "Step 0.5"
  key_links:
    - from: "explain/format/optimise Step 0.5"
      to: ".pbi-context.md ## Model Context"
      via: "Read tool check — skip ask if present"
      pattern: "Model Context"
---

<objective>
Add Step 0.5 (model context intake) to explain.md, format.md, and optimise.md.

Purpose: All DAX commands must ground their output in model context per DAX-01. The step is identical in shape for all three but with command-specific question phrasing. format.md gets a lightweight variant that does not block formatting if context is absent.
Output: Three updated command files, each with Step 0.5 prepended (between Step 0 and Step 1 for explain/optimise; before Step 1 for format which has no Step 0).
</objective>

<execution_context>
@C:/Users/DeveshD/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/DeveshD/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-context-aware-dax/02-CONTEXT.md
@.planning/phases/02-context-aware-dax/02-RESEARCH.md
@.claude/skills/pbi/commands/explain.md
@.claude/skills/pbi/commands/format.md
@.claude/skills/pbi/commands/optimise.md
</context>

<interfaces>
<!-- Per-command question phrasing (from 02-RESEARCH.md): -->
<!--
explain:   "Which table does this measure belong to?"
format:    "Which table does this measure belong to?" (lightweight — does not block if no answer; note "context not available — using pasted measure as-is")
optimise:  "Which table does this measure belong to, and are there any related tables involved?"
-->

<!-- Step placement by file: -->
<!--
explain.md: insert between Step 1 (Initial Response) and Step 2 (Measure Extraction)
            Actually: explain has Step 1 (Initial Response), Step 2 (Measure Extraction), Step 3 (Prior Failure Check)...
            Insert as Step 1.5 between Step 1 and Step 2 — OR insert before Step 1.
            BETTER: Add as a step after the analyst pastes their measure (Step 2 extracts it) so we have the measure name.
            Per research: "Step 0.5" fractional naming convention.
            explain has no Step 0 — first step is Step 1. Insert "Step 0.5" before Step 1 (before "Paste your DAX measure below:").

format.md:  No Step 0. Has step 1 "Measure Extraction". Insert Step 0.5 before Step 1.
            HOWEVER: format prompts "Paste your DAX measure below:" as its very first action (not in a step header).
            Insert Step 0.5 after receiving the pasted measure and before the extraction/format steps.
            Placement: after "Wait for the analyst to paste" and before Step 1 — label as "Step 0.5".

optimise.md: Similar to format — "Respond with: Paste..." then Step 1 (Measure Extraction).
             Insert Step 0.5 before Step 1.
-->

<!-- CRITICAL for format.md: does NOT block if context absent — note "context not available — using pasted measure as-is" and proceed. -->
<!-- This is the research pitfall: "format and optimise are paste-in transforms — if context absent, do NOT block execution" -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add Step 0.5 to explain.md</name>
  <files>.claude/skills/pbi/commands/explain.md</files>
  <action>
Read .claude/skills/pbi/commands/explain.md fully before making any changes.

explain.md starts with "Step 1 — Initial Response" which prompts "Paste your DAX measure below:" and waits. There is no Step 0.

Insert a new "Step 0.5 — Model Context Check" immediately before "Step 1 — Initial Response" (i.e., at the very top of the Instructions section, before the "Paste your DAX" prompt):

```
### Step 0.5 — Model Context Check

Read Session Context for `## Model Context` section.

- If `## Model Context` is present and non-empty: note the table context. Proceed to Step 1. Use the noted table when describing filter context in Step 5 output.
- If `## Model Context` is absent or empty:
  - Ask: "Which table does this measure belong to?"
  - Wait for the analyst's answer.
  - Read `.pbi-context.md` with Read tool. Add `## Model Context` section with the analyst's answer. Write back with Write tool.
  - Proceed to Step 1 using the noted table context.
```

Write the full file back. Steps 1 through 6 must remain unchanged.
  </action>
  <verify>
    <automated>grep -n "Step 0.5" ".claude/skills/pbi/commands/explain.md" && echo "EXPLAIN_STEP_05_PRESENT"</automated>
  </verify>
  <done>explain.md has "Step 0.5 — Model Context Check" before Step 1. Step 1 through Step 6 content is unchanged. The check skips the ask when Model Context is already present.</done>
</task>

<task type="auto">
  <name>Task 2: Add Step 0.5 to format.md and optimise.md</name>
  <files>.claude/skills/pbi/commands/format.md, .claude/skills/pbi/commands/optimise.md</files>
  <action>
Read both files fully before making any changes.

**For format.md:**

format.md opens with "Respond to the analyst with exactly: 'Paste your DAX measure below:'" followed by "Once the analyst pastes a DAX measure, follow these steps in order." then Step 1 (Measure Extraction).

Insert Step 0.5 between the "Once the analyst pastes..." line and Step 1. This step fires AFTER the analyst has pasted their measure (so it is context about the pasted measure, not a pre-paste ask):

```
### Step 0.5 — Model Context Check

Read Session Context for `## Model Context` section.

- If `## Model Context` is present and non-empty: note the table context. Use it when inferring display folder in Step 5 output. Proceed to Step 1.
- If `## Model Context` is absent or empty:
  - Ask: "Which table does this measure belong to? (optional — skip if you just want formatting)"
  - If the analyst answers: Read `.pbi-context.md` with Read tool. Add `## Model Context` section with the answer. Write back with Write tool. Use the table context in output.
  - If the analyst skips or says "skip": note "Table context not available — formatting pasted measure as-is." Proceed to Step 1 without blocking.
```

Write the full format.md back. Steps 1 through 6 must remain unchanged.

---

**For optimise.md:**

optimise.md opens with "Respond with: 'Paste your DAX measure below:'" then "Wait for the analyst to paste a DAX measure, then follow these steps in order." followed by Step 1 (Measure Extraction).

Insert Step 0.5 between the "Wait for the analyst..." line and Step 1:

```
### Step 0.5 — Model Context Check

Read Session Context for `## Model Context` section.

- If `## Model Context` is present and non-empty: note the table and related table context. Proceed to Step 1. Use this context when generating rationale for any rewrites.
- If `## Model Context` is absent or empty:
  - Ask: "Which table does this measure belong to, and are there any related tables involved?"
  - Wait for the analyst's answer.
  - Read `.pbi-context.md` with Read tool. Add `## Model Context` section with the answer. Write back with Write tool.
  - Proceed to Step 1 using the noted context.
```

Write the full optimise.md back. Steps 1 through 9 must remain unchanged.
  </action>
  <verify>
    <automated>grep -n "Step 0.5" ".claude/skills/pbi/commands/format.md" && grep -n "Step 0.5" ".claude/skills/pbi/commands/optimise.md" && echo "BOTH_UPDATED"</automated>
  </verify>
  <done>format.md has Step 0.5 that is non-blocking (analyst can skip context question). optimise.md has Step 0.5 that asks about table and related tables. Both check for existing Model Context before asking. Existing steps unchanged.</done>
</task>

</tasks>

<verification>
Run both verify commands. Then spot-check step numbering:
  grep -n "^### Step" ".claude/skills/pbi/commands/explain.md"
  grep -n "^### Step" ".claude/skills/pbi/commands/format.md"
  grep -n "^### Step" ".claude/skills/pbi/commands/optimise.md"

Expected: each file shows Step 0.5 present, all pre-existing step numbers still present.

Manual smoke test (using tests/phase2-acceptance-scenarios.md scenario S2-03):
- Run /pbi explain with empty .pbi-context.md → verify "Which table does this measure belong to?" is asked
- Run /pbi format with empty context → verify question is asked with skip option
- Run /pbi optimise with empty context → verify table + related tables question is asked
</verification>

<success_criteria>
- explain.md, format.md, and optimise.md each have Step 0.5 added
- format.md variant is non-blocking (analyst can skip the context question)
- All three skip the ask when ## Model Context is already present in .pbi-context.md
- No existing step numbers changed in any file
- No filter-sensitive detection added to these files (that is new.md only — per research pitfall 2)
</success_criteria>

<output>
After completion, create .planning/phases/02-context-aware-dax/02-03-SUMMARY.md
</output>
