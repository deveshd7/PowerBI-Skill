---
phase: 02-context-aware-dax
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/phase2-acceptance-scenarios.md
autonomous: true
requirements: [DAX-01, DAX-02, DAX-03, INTR-04, PHASE-02]

must_haves:
  truths:
    - "A manual test script exists covering all 14 Phase 2 test rows (DAX-01 through PHASE-02)"
    - "Each scenario has a clear pass criterion a human can evaluate without interpretation"
    - "Test file follows the same structure as tests/acceptance-scenarios.md from Phase 1"
  artifacts:
    - path: "tests/phase2-acceptance-scenarios.md"
      provides: "14-scenario manual acceptance test script for Phase 2 behaviors"
      contains: "DAX-01, DAX-02, DAX-03, INTR-04, PHASE-02"
  key_links:
    - from: "tests/phase2-acceptance-scenarios.md"
      to: "Phase 2 requirement IDs"
      via: "scenario requirement cross-reference column"
      pattern: "DAX-0[123]|INTR-04|PHASE-02"
---

<objective>
Create the Phase 2 acceptance test script that wave 1+ tasks will be verified against.

Purpose: Nyquist compliance — automated verify commands in plans 02-05 reference this file. It must exist before those plans execute so the human reviewer has a script to follow.
Output: tests/phase2-acceptance-scenarios.md with 14 scenarios covering all Phase 2 requirement IDs.
</objective>

<execution_context>
@C:/Users/DeveshD/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/DeveshD/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-context-aware-dax/02-CONTEXT.md
@.planning/phases/02-context-aware-dax/02-RESEARCH.md
@.planning/phases/02-context-aware-dax/02-VALIDATION.md
@tests/acceptance-scenarios.md
</context>

<interfaces>
<!-- Reference: Phase 1 acceptance scenario pattern (from tests/acceptance-scenarios.md) -->
<!-- Each scenario:
     - ID (e.g., S1-01)
     - Title
     - Requirements covered
     - Preconditions
     - Steps table: | Step | User Action | Expected Response |
     - Pass criteria
-->

<!-- .pbi-context.md schema (from 02-RESEARCH.md): -->
<!--
## Model Context     ← checked by Step 0.5 in all DAX commands
## Visual Context    ← new section to be added by filter-sensitive gate
## Business Question ← read by deep mode measures gate
## Command History   ← scanned by measures gate for /pbi new rows
-->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Write Phase 2 acceptance test scenarios</name>
  <files>tests/phase2-acceptance-scenarios.md</files>
  <action>
Create tests/phase2-acceptance-scenarios.md using the same format as tests/acceptance-scenarios.md (ID, title, requirements, preconditions, steps table, pass criteria). Write 14 scenarios covering all 14 test rows from the VALIDATION.md per-task verification map.

Organize into 4 behavior groups:

**Group 1: Context Intake (DAX-01) — 4 scenarios**
- S2-01: `/pbi new` with empty `.pbi-context.md` — verify "Which table should this measure go in, and which columns are relevant?" is asked before DAX output
- S2-02: `/pbi new` twice in same session — verify second invocation skips context question (reuse, no re-ask)
- S2-03: Each of explain/format/optimise/comment/error with empty context — verify each asks its command-specific context question before proceeding
- S2-04: `/pbi load` then `/pbi new` — verify new does NOT re-ask context (load context present, skip intake)

**Group 2: Duplication Check (DAX-02) — 2 scenarios**
- S2-05: `/pbi new` — verify "Does a similar measure already exist in the model?" appears before any DAX
- S2-06: `/pbi new` with "yes" answer — verify output uses CALCULATE wrapper pattern (e.g., `CALCULATE([ExistingMeasure], ...)`) and does not duplicate base logic

**Group 3: Filter-Sensitive Gate (DAX-03 + INTR-04) — 4 scenarios**
- S2-07: Request DATESYTD measure — verify "where will this be placed and what slicers are active?" fires BEFORE any DAX expression appears in output
- S2-08: Two consecutive time-intelligence measures — verify second measure skips visual context question (reuses saved context)
- S2-09: Request `SUM(Sales[Amount])` — verify NO visual context question is asked (non-filter-sensitive, no gate)
- S2-10: Request RANKX measure — verify visual context question fires before DAX (ratio/rank trigger)

**Group 4: Measures Gate in Deep Mode (PHASE-02) — 4 scenarios**
- S2-11: Complete a deep mode session and say "done" — verify measures session summary shows list of `/pbi new` measures from Command History
- S2-12: Measures gate output — verify business question from `## Business Question` in `.pbi-context.md` is restated in gate output
- S2-13: Answer "no" to "do these measures answer the business question?" — verify gate does NOT close the session, offers to continue generating
- S2-14: Answer "confirm" to close prompt — verify session closed message appears ("Deep mode session closed. Use /pbi diff or /pbi commit...")

**Precondition setup for each group:** Specify exactly what state `.pbi-context.md` must be in before running (empty file, file with Model Context populated, file with Visual Context populated, deep mode session in progress).

**Pass criteria:** Binary — either the stated response appears (pass) or it does not (fail). No judgment calls.

**Format note:** Copy the exact table structure from tests/acceptance-scenarios.md. Use `| Step | User Action | Expected Response |` column headers.
  </action>
  <verify>
    <automated>MISSING — this task creates the file. Verify: ls tests/phase2-acceptance-scenarios.md returns the file path.</automated>
  </verify>
  <done>tests/phase2-acceptance-scenarios.md exists with 14 scenarios in 4 groups, each scenario has requirement ID cross-references, steps table, and binary pass criteria. File follows Phase 1 scenario format.</done>
</task>

</tasks>

<verification>
Run: ls tests/phase2-acceptance-scenarios.md
Expected: File exists.

Manual check: Open file. Confirm 14 scenarios present. Confirm each has a steps table (| Step | User Action | Expected Response |). Confirm requirement IDs DAX-01, DAX-02, DAX-03, INTR-04, PHASE-02 each appear in at least one scenario's requirement coverage line.
</verification>

<success_criteria>
- tests/phase2-acceptance-scenarios.md exists with 14 scenarios
- All 5 Phase 2 requirement IDs covered (DAX-01, DAX-02, DAX-03, INTR-04, PHASE-02)
- Each scenario has binary pass/fail criteria a human reviewer can evaluate
- Scenario format matches Phase 1 pattern (ID, title, requirements, preconditions, steps table, pass criteria)
</success_criteria>

<output>
After completion, create .planning/phases/02-context-aware-dax/02-01-SUMMARY.md
</output>
