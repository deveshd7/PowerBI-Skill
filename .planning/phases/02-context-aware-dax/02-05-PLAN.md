---
phase: 02-context-aware-dax
plan: 05
type: execute
wave: 2
depends_on: [02-01, 02-02]
files_modified:
  - .claude/skills/pbi/commands/deep.md
autonomous: false
requirements: [PHASE-02]

must_haves:
  truths:
    - "When the analyst says 'done' in a deep mode session, the skill shows a summary of all /pbi new measures from Command History before closing"
    - "The session summary restates the Business Question from .pbi-context.md"
    - "The gate blocks session close if the analyst answers 'no' to the business question check"
    - "The gate closes the session on 'confirm' with the correct closure message"
    - "The gate does NOT fire after each individual measure -- only on analyst completion signal"
  artifacts:
    - path: ".claude/skills/pbi/commands/deep.md"
      provides: "Updated deep command with Step 4 (measures gate) as a terminal session step"
      contains: "Step 4 - Measures Gate"
  key_links:
    - from: ".claude/skills/pbi/commands/deep.md Step 4"
      to: ".pbi-context.md ## Command History"
      via: "read /pbi new rows from Command History"
      pattern: "/pbi new"
    - from: ".claude/skills/pbi/commands/deep.md Step 4"
      to: ".pbi-context.md ## Business Question"
      via: "restate business question in gate output"
      pattern: "Business Question"
---

<objective>
Add Step 4 (measures gate) to deep.md as a terminal session step that fires when the analyst signals completion.

Purpose: Implements PHASE-02 — the explicit gate before a deep mode session closes. Shows a measure summary, restates the business question, and requires confirmation before closing.
Output: Updated deep.md with Step 4 appended to the session flow. Existing Steps 0-3 and all anti-patterns unchanged.
</objective>

<execution_context>
@C:/Users/DeveshD/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/DeveshD/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-context-aware-dax/02-CONTEXT.md
@.planning/phases/02-context-aware-dax/02-RESEARCH.md
@.claude/skills/pbi/commands/deep.md
</context>

<interfaces>
<!-- Current deep.md step structure: -->
<!--
Step 0 -- Check Existing Context   (reads .pbi-context.md, shows what's on file)
Step 1 -- Full Upfront Intake      (collects Business Question, Data Model State, Existing Measures one at a time)
Step 2 -- Write Context            (writes Business Question, Existing Measures to .pbi-context.md)
Step 3 -- Confirm and Ready        (outputs summary, asks "What would you like to work on first?")
Anti-Patterns section
-->

<!-- What Step 4 adds: a terminal step that activates only on analyst completion signal -->
<!-- Activation signal: "done", "that's all the measures", "finished", "I think we're good", "complete" -->
<!-- Gate NEVER fires after each /pbi new call -- only on analyst signal -->

<!-- .pbi-context.md sections read by Step 4: -->
<!--
## Command History -- scan for rows with Command = /pbi new from this session
## Business Question -- restated in gate output
-->

<!-- Closure message (exact): -->
<!-- "Deep mode session closed. Use /pbi diff or /pbi commit to review and save your changes." -->

<!-- Step 3 must be updated to tell the analyst how to trigger the gate: -->
<!-- Add to Step 3 output: "When you've finished generating measures, say 'done' to review your session before closing." -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Append Step 4 (measures gate) and update Step 3 closing instruction in deep.md</name>
  <files>.claude/skills/pbi/commands/deep.md</files>
  <action>
Read .claude/skills/pbi/commands/deep.md fully before making any changes.

Make two changes:

**Change 1: Update Step 3 output template to tell the analyst how to close the session.**

In Step 3, find the output block that ends with "What would you like to work on first?" Append one line to that output block:

```
When you've finished generating measures, say "done" to review your session before closing.
```

So the full Step 3 output template becomes:
```
**Deep mode context gathered:**
- Business question: [summary]
- Model state: [summary or "from /pbi load"]
- Existing measures: [summary]

Context saved to .pbi-context.md. All /pbi commands will use this context going forward.

What would you like to work on first?
When you've finished generating measures, say "done" to review your session before closing.
```

**Change 2: Append Step 4 before the Anti-Patterns section.**

Insert the following after Step 3 and before the "### Anti-Patterns" section:

```
---

### Step 4 -- Measures Gate (end of session)

This step activates ONLY when the analyst signals completion of the measures phase.

**Trigger phrases (case-insensitive):** "done", "that's all the measures", "finished", "I think we're good", "complete", "all done", "that's it"

**Do NOT trigger** this step after each individual /pbi new call. Wait for explicit analyst signal.

When triggered:

1. Read `.pbi-context.md` with Read tool. Collect all rows from `## Command History` where Command = `/pbi new`.

2. Output the session summary:

   > **Measures session summary:**
   > [For each /pbi new row in Command History: "- [Measure Name] -> added in this session"]
   > (If no /pbi new rows found: "- No measures generated in this session via /pbi new.")
   >
   > **Business question on file:** [Content of ## Business Question from .pbi-context.md]
   >
   > Do these measures answer the stated business question? (yes / no -- if no, describe what's missing)

3. Wait for the analyst's answer.
   - **Yes** (or any affirmative): proceed to step 4.
   - **No**: acknowledge the gap. Output: "Understood -- what's missing? Continue with /pbi new to add more measures." Do NOT close the session. Resume from Step 3's "What would you like to work on first?" state.

4. Output the confirmation prompt:
   > All measures complete -- confirm to close the deep mode session. (confirm / cancel)

5. Wait for response.
   - **confirm**: Output: "Deep mode session closed. Use /pbi diff or /pbi commit to review and save your changes."
   - **cancel** (or anything else): Output: "Session kept open. Continue with /pbi new or other commands." Resume from Step 3 state.
```

Write the full file back. Steps 0-3 and the Anti-Patterns section must remain unchanged except for the Step 3 output addition noted above.

**Also update Anti-Patterns** to add three new anti-patterns at the end of the existing list:

```
- NEVER trigger the measures gate after each individual /pbi new call -- it fires only on analyst completion signal
- NEVER advance past the gate if the business question check returns "no" -- offer to continue generating
- NEVER impose visual or polish phases at the gate (Phase 3 scope)
```
  </action>
  <verify>
    <automated>grep -n "Step 4" ".claude/skills/pbi/commands/deep.md" && grep -n "Measures Gate" ".claude/skills/pbi/commands/deep.md" && grep -n "Business question on file" ".claude/skills/pbi/commands/deep.md" && echo "MEASURES_GATE_PRESENT"</automated>
  </verify>
  <done>deep.md has "Step 4 -- Measures Gate" after Step 3. Step 3 output template includes the "say done to review" instruction. Gate reads Command History for /pbi new rows and Business Question. Gate blocks on "no" answer. Gate closes session on "confirm". Anti-Patterns updated with 3 new entries. Steps 0-3 content unchanged.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification - Phase 2 smoke tests</name>
  <action>Run 4 smoke test scenarios from tests/phase2-acceptance-scenarios.md as described in how-to-verify below. Confirm all 4 pass before typing approved.</action>
  <what-built>
All 5 Phase 2 behaviors are now implemented across the 7 modified command files:
- Step 0.5 in explain.md, format.md, optimise.md, comment.md, error.md, new.md (model context intake)
- Step 2 (duplication check) and Step 2.5 (filter-sensitive gate) in new.md
- Step 4 (measures gate) in deep.md
- tests/phase2-acceptance-scenarios.md with 14 acceptance test scenarios
  </what-built>
  <how-to-verify>
Open the project in Claude Code and run the following scenarios from tests/phase2-acceptance-scenarios.md:

**Scenario S2-01 (DAX-01 -- context intake on /pbi new):**
1. Delete or empty .pbi-context.md
2. Run: /pbi new
3. Pass: "Which table should this measure go in, and which columns are relevant?" is asked BEFORE any DAX output

**Scenario S2-05 (DAX-02 -- duplication check):**
1. Describe a measure when prompted
2. Pass: "Does a similar measure already exist in the model?" appears BEFORE any DAX output

**Scenario S2-07 (DAX-03 / INTR-04 -- filter-sensitive gate):**
1. When prompted for measure description, say: "I want year-to-date revenue using DATESYTD"
2. Pass: "This measure is filter-sensitive -- where will it be placed..." appears BEFORE any DAX expression

**Scenario S2-11 (PHASE-02 -- deep mode measures gate):**
1. Run: /pbi deep
2. Provide a business question, data model, and existing measures when prompted
3. Run: /pbi new (create one measure)
4. Say: "done"
5. Pass: A session summary showing the measure name appears, followed by the business question restatement and the confirmation prompt

If all 4 smoke tests pass, type "approved". If any fail, describe which scenario failed and what the actual output was.
  </how-to-verify>
  <resume-signal>Type "approved" or describe which scenario(s) failed and what the actual output was</resume-signal>
</task>

</tasks>

<verification>
Run the grep verify command from Task 1.

Confirm step count in deep.md:
  grep -n "^### Step" ".claude/skills/pbi/commands/deep.md"
Expected: Steps 0, 1, 2, 3, 4 present.

Full suite verification: Run all 14 scenarios in tests/phase2-acceptance-scenarios.md and confirm all pass.
</verification>

<success_criteria>
- deep.md has Step 4 (Measures Gate) appended after Step 3
- Gate only activates on analyst completion signal (not after each /pbi new)
- Gate shows measure summary from Command History
- Gate restates Business Question from .pbi-context.md
- Gate blocks session close if business question check returns "no"
- Gate closes on "confirm" with exact message: "Deep mode session closed. Use /pbi diff or /pbi commit to review and save your changes."
- Human verification checkpoint passed for all 4 smoke test scenarios
- All 14 Phase 2 acceptance scenarios pass
</success_criteria>

<output>
After completion, create .planning/phases/02-context-aware-dax/02-05-SUMMARY.md
</output>
