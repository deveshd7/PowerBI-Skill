---
phase: 02-context-aware-dax
plan: 02
type: execute
wave: 1
depends_on: [02-01]
files_modified:
  - .claude/skills/pbi/commands/new.md
autonomous: true
requirements: [DAX-01, DAX-02, DAX-03, INTR-04]

must_haves:
  truths:
    - "Running /pbi new with empty .pbi-context.md causes the skill to ask about table and relevant columns before generating"
    - "Running /pbi new twice reuses stored Model Context without re-asking"
    - "Running /pbi new always asks the duplication check question before generating DAX"
    - "Answering yes to the duplication check produces a CALCULATE wrapper measure, not duplicated logic"
    - "Requesting a time-intelligence or ratio measure triggers the visual context question before DAX output"
    - "Running a second filter-sensitive measure reuses saved Visual Context without re-asking"
    - "Requesting SUM(Sales[Amount]) produces no visual context question"
  artifacts:
    - path: ".claude/skills/pbi/commands/new.md"
      provides: "Updated new command with Step 0.5 (context intake), Step 2 (duplication check), Step 2.5 (filter-sensitive gate)"
      contains: "Step 0.5"
  key_links:
    - from: ".claude/skills/pbi/commands/new.md Step 0.5"
      to: ".pbi-context.md ## Model Context"
      via: "Read tool check before asking"
      pattern: "Model Context"
    - from: ".claude/skills/pbi/commands/new.md Step 2.5"
      to: ".pbi-context.md ## Visual Context"
      via: "Read tool check before asking"
      pattern: "Visual Context"
---

<objective>
Rewrite commands/new.md to add all three Phase 2 context-awareness behaviors: model context intake (Step 0.5), duplication check (Step 2), and filter-sensitive pattern gate (Step 2.5).

Purpose: new.md is the primary measure-creation command. All three behaviors land here and new.md's patterns will be referenced by the acceptance test scenarios in plan 01.
Output: Updated new.md with three new steps integrated into existing flow without breaking existing step cross-references.
</objective>

<execution_context>
@C:/Users/DeveshD/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/DeveshD/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-context-aware-dax/02-CONTEXT.md
@.planning/phases/02-context-aware-dax/02-RESEARCH.md
@.claude/skills/pbi/commands/new.md
</context>

<interfaces>
<!-- Current new.md step structure (must be preserved, no renumbering): -->
<!--
Step 0 — Mode Detection
Step 1 — Collect Requirements
Step 2 — Model Context Check (PBIP_MODE=file only)   ← this step is REPLACED
Step 3 — Generate Measure Components
Step 4 — Output Preview
Step 5 — File Write (PBIP_MODE=file only)
Step 6 — Update Session Context
-->

<!-- Steps being INSERTED (fractional naming — do not renumber existing steps): -->
<!--
Step 0.5 — Model Context Check  (universal, replaces old Step 2 which was file-mode only)
Step 2    — Duplication Check   (new content; renumbered from research's "Step 2")
Step 2.5  — Filter-Sensitive Pattern Check (new)
-->

<!-- CRITICAL: Old Step 2 ("Model Context Check — PBIP_MODE=file only") is fully REPLACED.
     The new Step 0.5 is universal (all modes).
     Old Step 3 through Step 6 retain their numbers unchanged.
-->

<!-- .pbi-context.md schema additions required: -->
<!--
## Visual Context
- Visual type: [card | table | matrix | chart | slicer | unknown]
- Active slicers: [e.g., Date[Year], Product[Category]]
- Noted: [ISO 8601 timestamp]
-->

<!-- Filter-sensitive keyword trigger list (case-insensitive): -->
<!--
Time intelligence: DATEYTD, SAMEPERIODLASTYEAR, TOTALYTD, DATESYTD, DATEADD, PARALLELPERIOD, DATESBETWEEN
Phrases: "year to date", "same period last year", "prior year", "month-to-date"
Ratio/rank: DIVIDE, RANKX, TOPN, PERCENTILEX
Phrases: "as a percentage", "ratio", "rank", "top N", "share of"
-->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add Step 0.5 (model context intake) and replace Step 2 in new.md</name>
  <files>.claude/skills/pbi/commands/new.md</files>
  <action>
Read .claude/skills/pbi/commands/new.md fully before making any changes.

Make two changes:

**Change 1: Insert Step 0.5 between Step 0 and Step 1.**

Add this new step immediately after Step 0 (Mode Detection) and before Step 1 (Collect Requirements):

```
### Step 0.5 — Model Context Check

Read Session Context for `## Model Context` section.

- If `## Model Context` is present and non-empty: note the table/column context. Proceed to Step 1 using this context — do NOT ask again.
- If `## Model Context` is absent or empty:
  - Ask: "Which table should this measure go in, and which columns are relevant?"
  - Wait for the analyst's answer.
  - Read `.pbi-context.md` with Read tool. Add `## Model Context` section with the analyst's answer. Write back with Write tool.
  - Proceed to Step 1 using the newly stored context.

Note: If model context was provided via `/pbi load` prior to this session, it will already be present — do not overwrite it.
```

**Change 2: Replace the existing Step 2 ("Model Context Check — PBIP_MODE=file only") with the new Duplication Check.**

Delete the old Step 2 body entirely. Replace with:

```
### Step 2 — Duplication Check

Ask:

> "Does a similar measure already exist in the model?"

Wait for the analyst's answer.

- **No** (or "not sure"): proceed to Step 2.5.
- **Yes**: ask "What's the existing measure? (paste its DAX or name it)"
  - Wait for answer.
  - In Step 3, generate a measure that wraps the existing one rather than duplicating the base logic. Use CALCULATE or a similar wrapper pattern. Example:
    ```dax
    Revenue YTD (Filtered) =
    CALCULATE(
        [Revenue YTD],
        Product[Category] = "Electronics"
    )
    ```
  - In Step 4 output, add under Assumptions: "Wraps existing measure [Name] — extends rather than duplicates logic."
  - Proceed to Step 2.5.
```

Write the full updated file back. Do not change Step 3, Step 4, Step 5, or Step 6. Do not renumber existing steps.
  </action>
  <verify>
    <automated>grep -n "Step 0.5" ".claude/skills/pbi/commands/new.md" && grep -n "Duplication Check" ".claude/skills/pbi/commands/new.md" && echo "BOTH_STEPS_PRESENT"</automated>
  </verify>
  <done>new.md contains "Step 0.5 — Model Context Check" as a universal check (not PBIP_MODE=file only). Old Step 2 ("Model Context Check (PBIP_MODE=file only)") is gone. New Step 2 is "Duplication Check". Steps 3-6 are unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Add Step 2.5 (filter-sensitive pattern gate) to new.md</name>
  <files>.claude/skills/pbi/commands/new.md</files>
  <action>
Read .claude/skills/pbi/commands/new.md (which now has Steps 0.5 and 2 from Task 1).

Insert Step 2.5 between Step 2 (Duplication Check) and Step 3 (Generate Measure Components):

```
### Step 2.5 — Filter-Sensitive Pattern Check

Scan the analyst's stated business intent from Step 1 for these patterns (case-insensitive):

- **Time intelligence keywords:** DATEYTD, SAMEPERIODLASTYEAR, TOTALYTD, DATESYTD, DATEADD, PARALLELPERIOD, DATESBETWEEN
- **Time intelligence phrases:** "year to date", "same period last year", "prior year", "month-to-date", "MTD", "YTD"
- **Ratio/rank keywords:** DIVIDE, RANKX, TOPN, PERCENTILEX
- **Ratio/rank phrases:** "as a percentage", "ratio", "rank", "top N", "share of", "% of"

If **no pattern detected**: proceed directly to Step 3.

If **pattern detected**:
1. Check Session Context for `## Visual Context` section.
   - If present and non-empty:
     Output: "Using saved visual context: [visual type] with slicers [slicer list]."
     Proceed to Step 3 using this context.
   - If absent or empty:
     Ask before generating:
     > "This measure is filter-sensitive — where will it be placed (e.g., card, table, matrix) and what slicers or date filters will be active?"
     Wait for the analyst's answer.
     Read `.pbi-context.md` with Read tool. Add `## Visual Context` section:
     ```
     ## Visual Context
     - Visual type: [from analyst's answer]
     - Active slicers: [from analyst's answer]
     - Noted: [current ISO 8601 timestamp]
     ```
     Write back with Write tool.
     Proceed to Step 3, incorporating the visual context into the DAX design (e.g., if placed in a card, consider CALCULATE with explicit date filter; if in a matrix with row context, note the slicer interactions).
```

Write the full updated file back. Steps 3-6 must remain at their exact positions and with their exact content.
  </action>
  <verify>
    <automated>grep -n "Step 2.5" ".claude/skills/pbi/commands/new.md" && grep -n "Filter-Sensitive" ".claude/skills/pbi/commands/new.md" && grep -n "Visual Context" ".claude/skills/pbi/commands/new.md" && echo "FILTER_GATE_PRESENT"</automated>
  </verify>
  <done>new.md contains "Step 2.5 — Filter-Sensitive Pattern Check" with the full keyword list, the visual context check, and the write-back instructions. Steps 3-6 are unchanged and at the correct positions.</done>
</task>

</tasks>

<verification>
Run both verify commands above. Then run:
  grep -c "^### Step" ".claude/skills/pbi/commands/new.md"
Expected: 8 steps total (0, 0.5, 1, 2, 2.5, 3, 4, 5, 6 = 9 step headers — any count >= 8 is acceptable).

Manual smoke test (using tests/phase2-acceptance-scenarios.md scenarios S2-01, S2-05, S2-07):
- S2-01: Run /pbi new with empty .pbi-context.md. Verify context question asked.
- S2-05: Run /pbi new. Verify duplication question asked before DAX output.
- S2-07: Request a DATESYTD measure. Verify visual context question fires before any DAX.
</verification>

<success_criteria>
- new.md has Step 0.5 (universal model context check, works in paste and file mode)
- new.md old Step 2 (PBIP_MODE=file only context check) is replaced with Step 2 (duplication check)
- new.md has Step 2.5 (filter-sensitive gate with keyword list and visual context write-back)
- Steps 3, 4, 5, 6 content unchanged
- Model Context is never overwritten if already present (load then new scenario works)
- Visual Context is reused across filter-sensitive measures in the same session
</success_criteria>

<output>
After completion, create .planning/phases/02-context-aware-dax/02-02-SUMMARY.md
</output>
