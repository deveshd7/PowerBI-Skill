---
phase: 02-context-aware-dax
plan: 04
type: execute
wave: 1
depends_on: [02-01]
files_modified:
  - .claude/skills/pbi/commands/comment.md
  - .claude/skills/pbi/commands/error.md
autonomous: true
requirements: [DAX-01]

must_haves:
  truths:
    - "Running /pbi comment with empty .pbi-context.md causes the skill to ask which table the measure belongs to"
    - "Running /pbi error with empty .pbi-context.md causes the skill to ask which table and involved columns"
    - "Both commands skip the context question when Model Context is already present"
  artifacts:
    - path: ".claude/skills/pbi/commands/comment.md"
      provides: "Updated comment with Step 0.5 (model context check)"
      contains: "Step 0.5"
    - path: ".claude/skills/pbi/commands/error.md"
      provides: "Updated error with Step 0.5 (model context check)"
      contains: "Step 0.5"
  key_links:
    - from: "comment/error Step 0.5"
      to: ".pbi-context.md ## Model Context"
      via: "Read tool check — skip ask if present"
      pattern: "Model Context"
---

<objective>
Add Step 0.5 (model context intake) to comment.md and error.md, completing the context intake rollout across all six DAX command files.

Purpose: comment and error are the final two of six DAX commands that need Step 0.5. After this plan, all DAX commands check for model context before proceeding.
Output: Two updated command files with Step 0.5 inserted at the correct position in each file's step flow.
</objective>

<execution_context>
@C:/Users/DeveshD/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/DeveshD/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-context-aware-dax/02-CONTEXT.md
@.planning/phases/02-context-aware-dax/02-RESEARCH.md
@.claude/skills/pbi/commands/comment.md
@.claude/skills/pbi/commands/error.md
</context>

<interfaces>
<!-- Per-command question phrasing (from 02-RESEARCH.md): -->
<!--
comment:  "Which table does this measure belong to?"
error:    "Which table is this measure in, and what are the involved columns or tables?"
-->

<!-- Step placement: -->
<!--
comment.md: Has a "File Mode Branch" section at the top before "## Instructions".
            The Instructions section starts with "Step 1 — Initial Response".
            There is no Step 0.
            Insert Step 0.5 inside ## Instructions before Step 1.
            IMPORTANT: The File Mode Branch is above ## Instructions — do NOT insert Step 0.5 in the File Mode Branch.
            Step 0.5 goes between "## Instructions" header and "### Step 1" inside that section.

error.md:   Has a "File Mode Branch" section before "## Instructions" equivalent (the step-named sections).
            Step 1 in error.md is "Step 1: Prompt for input".
            Insert Step 0.5 in the main instruction flow before Step 1.
            IMPORTANT: Do NOT insert in the File Fix Preview section at the top.
-->

<!-- comment.md has autocommit in the File Write-Back section — do NOT touch that section -->
<!-- error.md has File Fix Preview and Confirm section at the top — do NOT touch that section -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add Step 0.5 to comment.md</name>
  <files>.claude/skills/pbi/commands/comment.md</files>
  <action>
Read .claude/skills/pbi/commands/comment.md fully before making any changes.

comment.md structure:
- File Mode Branch section (at top)
- ## Instructions section containing:
  - Step 1 — Initial Response ("Paste your DAX measure below:")
  - Step 2 — Measure Extraction
  - Step 3 — Prior Failure Check
  - Step 4 — Comment Placement Rules
  - Step 5 — Description Field Generation Rules
  - Step 6 — Output Structure
  - Step 7 — Context Update

Insert a new Step 0.5 immediately after the "## Instructions" header and before "### Step 1 — Initial Response":

```
### Step 0.5 — Model Context Check

Read Session Context for `## Model Context` section.

- If `## Model Context` is present and non-empty: note the table context. Use it to make inline comments more specific (e.g., reference actual column names when explaining filter logic). Proceed to Step 1.
- If `## Model Context` is absent or empty:
  - Ask: "Which table does this measure belong to?"
  - Wait for the analyst's answer.
  - Read `.pbi-context.md` with Read tool. Add `## Model Context` section with the analyst's answer. Write back with Write tool.
  - Proceed to Step 1 using the noted table context.
```

Write the full file back. The File Mode Branch section, Steps 1 through 7, and the File Write-Back section must remain unchanged.
  </action>
  <verify>
    <automated>grep -n "Step 0.5" ".claude/skills/pbi/commands/comment.md" && echo "COMMENT_STEP_05_PRESENT"</automated>
  </verify>
  <done>comment.md has "Step 0.5 — Model Context Check" inside the ## Instructions section, before Step 1. File Mode Branch section unchanged. Steps 1-7 and File Write-Back unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Add Step 0.5 to error.md</name>
  <files>.claude/skills/pbi/commands/error.md</files>
  <action>
Read .claude/skills/pbi/commands/error.md fully before making any changes.

error.md structure:
- Header paragraph ("You are a Power BI error diagnosis expert...")
- File Mode Branch section
- File Fix Preview and Confirm section
- The main step flow starting with "### Step 1: Prompt for input"

The main instruction steps are:
  - Step 1: Prompt for input ("Paste your Power BI error message or error log below:")
  - Step 2: Prior error check
  - Step 3: Last-command correlation
  - Step 4: Classify and diagnose
  - Step 5: Output structure
  - Step 6: Update .pbi-context.md

Insert Step 0.5 before "### Step 1: Prompt for input" (immediately after the File Fix Preview and Confirm section ends and before the main step flow begins):

```
### Step 0.5 — Model Context Check

Read Session Context for `## Model Context` section.

- If `## Model Context` is present and non-empty: note the table and column context. Use it to sharpen diagnosis in Step 4 (e.g., confirm referenced column names exist in the described model). Proceed to Step 1.
- If `## Model Context` is absent or empty:
  - Ask: "Which table is this measure in, and what are the involved columns or tables?"
  - Wait for the analyst's answer.
  - Read `.pbi-context.md` with Read tool. Add `## Model Context` section with the analyst's answer. Write back with Write tool.
  - Proceed to Step 1 using the noted context.
```

Write the full file back. The header paragraph, File Mode Branch, File Fix Preview and Confirm, and Steps 1 through 6 must remain unchanged.
  </action>
  <verify>
    <automated>grep -n "Step 0.5" ".claude/skills/pbi/commands/error.md" && echo "ERROR_STEP_05_PRESENT"</automated>
  </verify>
  <done>error.md has "Step 0.5 — Model Context Check" before Step 1. File Mode Branch and File Fix Preview sections unchanged. Steps 1-6 unchanged.</done>
</task>

</tasks>

<verification>
Run both verify commands. Then confirm all six DAX command files now have Step 0.5:
  for f in explain format optimise comment error new; do grep -l "Step 0.5" ".claude/skills/pbi/commands/$f.md"; done
Expected: All 6 file paths printed.

Manual smoke test (scenario S2-03 remaining commands from tests/phase2-acceptance-scenarios.md):
- Run /pbi comment with empty .pbi-context.md → verify "Which table does this measure belong to?" is asked
- Run /pbi error with empty .pbi-context.md → verify "Which table is this measure in, and what are the involved columns or tables?" is asked
</verification>

<success_criteria>
- comment.md and error.md each have Step 0.5 added in the correct position
- Both skip the ask when ## Model Context is already present
- No existing step numbers or content changed in either file
- File Mode Branch sections in both files untouched
- All 6 DAX command files now have Step 0.5 (confirmed by the shell loop above)
</success_criteria>

<output>
After completion, create .planning/phases/02-context-aware-dax/02-04-SUMMARY.md
</output>
