---
phase: 03-context-field-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/pbi/commands/diff.md
  - .claude/skills/pbi/commands/commit.md
autonomous: true
requirements: [DEBT-02]

must_haves:
  truths:
    - "User running pbi-diff sees `- Measure:` field written in `## Last Command` with changed measure names (not `(git operation)`)"
    - "User running pbi-commit sees `- Measure:` field written in `## Last Command` with the committed measure names (not `(git operation)`)"
    - "pbi-error can read a measure name from `## Last Command` after a pbi-diff or pbi-commit session"
  artifacts:
    - path: ".claude/skills/pbi/commands/diff.md"
      provides: "Schema-correct `## Last Command` write with Measure field containing changed measure names"
      contains: "Measure:"
    - path: ".claude/skills/pbi/commands/commit.md"
      provides: "Schema-correct `## Last Command` write with Measure field containing committed measure names"
      contains: "Measure:"
  key_links:
    - from: ".claude/skills/pbi/commands/diff.md"
      to: ".pbi-context.md"
      via: "Step 5 write instruction"
      pattern: "Measure: \\[changed measures\\]"
    - from: ".claude/skills/pbi/commands/commit.md"
      to: ".pbi-context.md"
      via: "Step 5 write instruction"
      pattern: "Measure: \\[committed measures\\]"
---

<objective>
Fix the missing/incorrect `Measure:` field in pbi-diff and pbi-commit's `.pbi-context.md` write instructions so pbi-error can correlate the correct measure after a diff or commit workflow.

Purpose: Both diff.md and commit.md currently write `Measure = (git operation)` to `## Last Command`. The schema field is `Measure:` and it should contain actual measure names parsed from the diff, not a generic placeholder. pbi-error reads this field to find the measure name when diagnosing an error.

Output: Two updated command files with schema-correct Measure field writes that surface measure names from the parsed diff.
</objective>

<execution_context>
@C:/Users/DeveshD/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/DeveshD/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.claude/skills/pbi/commands/diff.md
@.claude/skills/pbi/commands/commit.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix diff.md — write actual changed measure names to `Measure:` field in Step 5</name>
  <files>.claude/skills/pbi/commands/diff.md</files>
  <action>
Read `.claude/skills/pbi/commands/diff.md` and locate Step 5: Update .pbi-context.md (around line 122).

The current `## Last Command` write instruction (around line 126) is:
```
1. Update `## Last Command`: Command = `/pbi diff`, Timestamp = current UTC, Measure = `(git operation)`, Outcome = `Diff shown — [N] changes`.
```

The value `(git operation)` is a placeholder that does not contain a measure name. pbi-diff already parses changed measure names in Step 3 — those names are available at the time Step 5 runs.

Replace the `## Last Command` update line with an explicit schema-correct format that surfaces the parsed measure names:

```
1. Update `## Last Command` with these four lines in this exact order:
   - Command: /pbi diff
   - Timestamp: [current UTC ISO 8601]
   - Measure: [comma-separated list of changed measure names from Step 3 parse, or "(no measures changed)" if diff showed no measure changes]
   - Outcome: Diff shown — [N] changes
```

The `## Command History` append instruction (line 127) does not need to change — only the `## Last Command` block needs updating.

Write the full file back with only the `## Last Command` block in Step 5 updated. All other content must be preserved exactly.
  </action>
  <verify>
    <automated>grep -n "Measure:" ".claude/skills/pbi/commands/diff.md"</automated>
  </verify>
  <done>`diff.md` Step 5 `## Last Command` block contains `- Measure: [comma-separated list of changed measure names...]` — the `(git operation)` placeholder is gone and actual measure name population is instructed.</done>
</task>

<task type="auto">
  <name>Task 2: Fix commit.md — write committed measure names to `Measure:` field in Step 5</name>
  <files>.claude/skills/pbi/commands/commit.md</files>
  <action>
Read `.claude/skills/pbi/commands/commit.md` and locate Step 5: Context update (around line 146).

The current `## Last Command` write instruction (around line 150) is:
```
1. Update `## Last Command`: Command = `/pbi commit`, Timestamp = current UTC, Measure = `(git operation)`, Outcome = the commit subject line.
```

The value `(git operation)` is a placeholder. pbi-commit already parses changed measure names in Step 3 — those names are available at the time Step 5 runs.

Note: Step 5 is also reached from Step 1a and Step 1b (initial commit flows) where no measure parse has occurred. For these paths, use `(initial commit — no prior measures parsed)` as the Measure value.

Replace the `## Last Command` update line with:

```
1. Update `## Last Command` with these four lines in this exact order:
   - Command: /pbi commit
   - Timestamp: [current UTC ISO 8601]
   - Measure: [comma-separated list of measure names from Step 3 parse; or "(initial commit)" if arriving from Step 1a or Step 1b]
   - Outcome: [commit subject line, or "chore: initial PBIP model commit" for initial commit flows]
```

The `## Command History` append instruction does not need to change — only the `## Last Command` block needs updating.

Write the full file back with only the `## Last Command` block in Step 5 updated. All other content must be preserved exactly.
  </action>
  <verify>
    <automated>grep -n "Measure:" ".claude/skills/pbi/commands/commit.md"</automated>
  </verify>
  <done>`commit.md` Step 5 `## Last Command` block contains `- Measure: [comma-separated list of measure names...]` — the `(git operation)` placeholder is gone and actual measure name population is instructed, with a fallback value for initial commit paths.</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. Confirm git operation placeholder is gone from diff.md:
   `grep "(git operation)" ".claude/skills/pbi/commands/diff.md"` — must return no matches

2. Confirm git operation placeholder is gone from commit.md:
   `grep "(git operation)" ".claude/skills/pbi/commands/commit.md"` — must return no matches

3. Confirm Measure field is present in diff.md Step 5:
   `grep -n "Measure:" ".claude/skills/pbi/commands/diff.md"` — must return a match in Step 5

4. Confirm Measure field is present in commit.md Step 5:
   `grep -n "Measure:" ".claude/skills/pbi/commands/commit.md"` — must return a match in Step 5
</verification>

<success_criteria>
- DEBT-02 closed: both diff.md and commit.md Step 5 write actual measure names (from the parsed diff) into the `Measure:` field of `## Last Command`, replacing the `(git operation)` placeholder
- pbi-error can read `## Last Command` after a pbi-diff or pbi-commit session and find a measure name in the `Measure:` field
- Initial commit paths in commit.md use an explicit fallback value `(initial commit)` rather than leaving Measure empty or containing `(git operation)`
</success_criteria>

<output>
After completion, create `.planning/phases/03-context-field-fixes/03-02-SUMMARY.md`
</output>
